%h1 Listing checkouts

%table
  %tr
    %th
    %th
    %th
    %th

  - @checkouts.each do |checkout|
    %tr
      %td= checkout.id
      %td= link_to 'Scan', scan_checkout_path(checkout)
      %td= link_to 'Total', total_checkout_path(checkout)
      %td= link_to 'Destroy', checkout, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Checkout', new_checkout_path
